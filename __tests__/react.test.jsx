/* eslint-disable */

// import React from 'react';
// import { render, fireEvent } from '@testing-library/react';
// import runTabsTests from './tests/runTabsTests';

// import Tabs from '../packages/precept-react/src/Tabs';

describe('react', () => {
    it('should do something', () => {
        expect(true).toBe(true);
    });
    // runTabsTests({
    //     fireEvent,
    //     render(data) {
    //         const { tabs, panels, options = {} } = data;

    //         return render(
    //             <Tabs {...options}>
    //                 <Tabs.Tablist>
    //                     {tabs.map((tab) => (
    //                         <Tabs.Tab key={tab}>{tab}</Tabs.Tab>
    //                     ))}
    //                 </Tabs.Tablist>
    //                 {panels.map((panel) => (
    //                     <Tabs.Panel key={panel}>{panel}</Tabs.Panel>
    //                 ))}
    //             </Tabs>
    //         );
    //     },
    // });
});
